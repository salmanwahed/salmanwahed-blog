# Django is pinned explicitly. It used to arrive transitively via
# django-compressor, which meant a compressor release could move the framework
# under us without a line changing here.
Django==5.2.17

beautifulsoup4==4.15.0
django-compressor==4.6.0
django-debug-toolbar==8.0.0
gunicorn==26.2.0
hiredis==3.4.1
markdown==3.10.3
pillow==12.3.0
# psycopg 3 replaces psycopg2-binary: it is the adapter Django 5.x is built
# against, and it ships wheels for 3.13 where psycopg2 needs a compiler.
psycopg[binary]==3.3.5
pygments==2.21.0
python-dotenv==1.2.3
redis==8.1.0
sentry-sdk==2.68.1
